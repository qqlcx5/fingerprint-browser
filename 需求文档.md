# 多账号环境隔离浏览器 — 需求文档

> 版本 v1.2 / 2026-09-08 / 内部资料
>
> v1.2 变更：按已实现代码回写 IPC 契约（统一 `Result<T>` 返回信封；新增 `env:get`、`align:confirm`、`app:notices`、`app:wipeData`、`env:crashed`）；明确国家变更确认在下次启动生效；macOS arm64 目录包与 DMG 包内 E2E 已验证。

## 1. 项目定位

桌面端多账号浏览器管理工具，面向电商客服、社媒运营等合法多账号场景。

核心能力：**账号环境隔离 + 独立代理 IP 绑定 + 轻量指纹差异化**。

不做：深度防关联（对抗 Cloudflare/银行级风控）。产品宣传禁止使用"防封号""绕过风控"等表述。

## 2. 术语表

| 术语 | 含义 |
|---|---|
| 环境（Env） | 一条独立的浏览器身份：独立数据目录 + 指纹 + 代理配置 |
| 核心指纹 | 创建时生成且此后不变的字段：UA、平台、GPU/WebGL、屏幕分辨率、硬件并发数、设备内存等 |
| 对齐字段 | 跟随代理出口地区更新的字段：时区、语言（Accept-Language）、地理位置 |
| 内核 | Playwright 管理的 Chromium 二进制，所有环境共享，版本全局锁定 |

## 3. 技术选型（全部宽松协议，可商用分发）

| 组件 | 选型 | 协议 |
|---|---|---|
| 界面壳 | Electron | MIT |
| 脚手架 | electron-vite（create-electron，Vue3 + TS） | MIT |
| 启动层 | playwright-core | Apache-2.0 |
| 浏览器内核 | Playwright 管理的 Chromium 二进制 | BSD-3 |
| 指纹差异化 | apify/fingerprint-generator + fingerprint-injector | MIT/Apache-2.0 |
| 本地存储 | SQLite（better-sqlite3） | MIT |
| 打包 | electron-builder | MIT |

明确不使用：CloakBrowser（二进制禁止再分发）、VirtualBrowser（内核授权不明）。

**版本锁定策略**：`playwright-core` 版本与 Chromium revision 一一对应，锁定在 package.json；升级内核 = 升级 playwright-core + 重新回归测试，作为独立发布流程处理。

## 4. 架构

```
Electron 渲染进程（Vue3 界面）
    ↕ IPC（preload 桥接，contextIsolation 开启，白名单 API）
Electron 主进程
    ├─ 环境管理（CRUD，SQLite）
    ├─ 内核管理（下载/校验/版本锁定，存 userData/chromium/{revision}/）
    ├─ launchEnv(envId) → playwright-core + fingerprint-injector
    │     ├─ 每环境独立 userDataDir
    │     ├─ proxy 配置注入（含认证）
    │     └─ init script 注入指纹与对齐字段
    └─ 生命周期管理（运行状态表、孤儿进程清理）
```

### IPC 契约（preload 暴露 `window.api`）

所有 invoke 通道统一返回 `Result<T>`，而非跨 IPC 抛异常：

```ts
{ ok: true, data: T } | { ok: false, error: { code, message } }
```

渲染层按 `ok` 分支处理；`code` 统一映射为 §9 的用户可见错误文案。代理密码、数据库密钥等敏感数据永不回传渲染层。

| 通道 | 方向 | 参数 → `data`（失败统一为 `Result.error`） |
|---|---|---|
| `app:ping` | invoke（内部自检） | → `{pong, version, arch, sqlite}` |
| `env:list` | invoke | → `EnvSummary[]`（列表摘要，不含指纹与密码） |
| `env:get` | invoke | `{id}` → `Env \| null`（编辑详情；代理仅回 `hasPassword`，不回密码） |
| `env:create` | invoke | `{name, remark?, proxyConfig?}` → `Env`；代理测连失败仍允许保存，以直连基准生成指纹 |
| `env:update` | invoke | `{id, name?, remark?, proxyConfig?}` → `Env`；代理地区检测收敛到 `env:start` |
| `env:delete` | invoke | `{id}` → `{id}`；运行中环境返回 `ENV_RUNNING` |
| `env:start` | invoke | `{id}` → `CountryChangeInfo \| null`；`null` 表示已直接启动，非空表示已启动但代理地区变化待确认 |
| `env:stop` | invoke | `{id}` → `{id}`；优雅关闭，5s 超时按环境 profile 强杀进程树 |
| `env:status` | invoke | → `Record<id, EnvStatus>` |
| `align:confirm` | invoke | `{id, accept}` → `{id}`；确认时仅更新对齐字段，**下次启动生效** |
| `proxy:test` | invoke | `ProxyConfig` → `{ip, country, latencyMs}` |
| `browser:ensure` | invoke | → `{ready, revision, path}`；缺失时触发下载并推送进度 |
| `app:notices` | invoke | → `StartupNotice[]`；启动期 db 重置/弱加密提示，读取后清空 |
| `app:wipeData` | invoke | → `{wipedEnvs}`；停止全部环境后删除 db 与 `envs/`，保留内核和日志 |
| `env:status-changed` | event | 主进程推送完整状态表 |
| `env:crashed` | event | `{envId, exitCode}`；渲染层提示并引导查日志 |
| `browser:download-progress` | event | `{received, total}` |

**国家变更时序**：`env:start` 启动前强制代理测连；出口国家与当前对齐字段不一致时，先按旧对齐字段启动并返回 `CountryChangeInfo`。用户确认 `align:confirm` 后，只有时区/语言/定位等派生字段写入数据库，核心指纹不变；更新从**下一次启动**开始生效。

## 5. 数据模型

### environments 表（SQLite）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | TEXT PK | uuid v4，同时用于目录名 |
| name | TEXT NOT NULL | 显示名 |
| remark | TEXT | 备注，默认 '' |
| fingerprint | TEXT | 核心指纹 JSON（生成后只读） |
| align_fields | TEXT | 对齐字段 JSON：`{timezone, language, geolocation?}` |
| proxy_config | TEXT | JSON：`{type:'http'\|'https'\|'socks5', host, port, username?, password?}`；密码加密存储（见 §10） |
| created_at / updated_at / last_launched_at | INTEGER | 时间戳 |

运行状态**不入库**：仅存主进程内存，应用启动时所有环境初始化为 idle（避免崩溃后残留脏状态）。

### 存储路径

| 内容 | 路径 |
|---|---|
| 数据库 | `app.getPath('userData')/data/envs.db` |
| 环境数据目录 | `userData/envs/{id}/profile`（即 Chromium userDataDir） |
| 环境下载目录 | `userData/envs/{id}/downloads`（默认隔离，防止多环境下载互串） |
| 内核 | `userData/chromium/{revision}/` |
| 日志 | `userData/logs/`（滚动：5MB × 5 个文件） |

## 6. 功能需求

### P0（MVP，必须有）

#### 6.1 环境管理
创建/重命名/编辑备注/删除；列表页展示名称、代理状态、运行状态、最后启动时间。删除 = 删库记录 + 删整个环境目录（二次确认，明确提示不可恢复）。

#### 6.2 浏览器内核管理（首启前置流程）
- 首次启动检测内核是否存在（revision 目录 + 可执行文件校验）
- 缺失 → 内核下载页：进度条、体积提示（约 150–300MB）、失败可重试（断点续传）
- 下载前检查磁盘剩余空间 ≥ 2GB
- 磁盘空间不足、网络失败给出明确报错与"重试"按钮
- 内核损坏（校验失败）→ 引导重新下载

#### 6.3 环境隔离
每环境独立 `userDataDir`：cookie、localStorage、登录态、缓存、下载目录完全隔离且持久化。隔离边界 = 目录级，不依赖浏览器内任何隔离机制。

#### 6.4 代理配置
- 每环境支持 HTTP/HTTPS/SOCKS5，含账号密码认证
- 启动前强制连通性测试：经代理请求 IP 查询接口，返回出口 IP、国家、延迟
- 错误分类：DNS 解析失败 / 连接超时 / 认证失败 / 代理协议错误，分别给出可读提示
- 不配置代理的环境允许以直连启动（界面标注"直连"）

#### 6.5 指纹策略（两段式）
1. **核心指纹**：创建环境时以当时代理出口地区为输入，用 fingerprint-generator 生成一次，存 SQLite，**此后永久只读，任何流程不得改写**（P1 的手动编辑是唯一例外入口）。
2. **对齐字段**：时区、语言、地理位置属于派生配置，跟随代理。**更换代理且出口国家变化时**：弹窗告知"地区已从 A 变为 B"，用户确认后仅更新对齐字段，核心指纹不动；用户取消则保持原对齐字段。
3. 一致性校验：生成核心指纹时，UA 平台/语言需与目标地区合理匹配（由 fingerprint-generator 的 locale 入参保证），避免"中文 UA + 巴西 IP"类矛盾。

#### 6.6 启动/停止与生命周期
- 启动流程：校验内核 → 测试代理 → 注入指纹 → `chromium.launchPersistentContext(userDataDir, {proxy, executablePath})` → 打开窗口
- **关闭浏览器窗口 = 停止该环境**（Chromium 进程退出，数据已落盘）
- 应用退出 = 逐个优雅停止所有运行中环境（5s 超时强杀）
- 崩溃恢复：主进程启动时按 userDataDir 路径匹配命令行，清理孤儿 Chromium 进程
- 每环境使用 Playwright `--remote-debugging-pipe` 管道通信，无需分配调试端口，避免并行端口冲突

#### 6.7 多环境并行
同时运行 ≥10 个环境互不影响；界面实时显示每个环境运行状态（事件推送，非轮询）。

### P1（第二迭代）

7. 环境分组/搜索/批量删除
8. 环境配置导出/导入（JSON，不含 cookie 与密码）
9. 核心指纹手动编辑（UA、分辨率等，含合法性校验，防"不可能组合"）
10. 开机启动、系统托盘（主窗口关闭时驻留托盘而非退出）
11. 日志查看页（方便客服排障）

### P2（商业化阶段）

12. 云端同步环境配置
13. 团队协作与权限
14. 授权/计费（按环境数订阅）
15. 自动更新（electron-updater）

## 7. 非功能需求

- 平台：Windows x64 优先，macOS arm64 次之，Linux 兼容
- 启动单个环境 ≤ 5s（内核就绪、不含代理测试耗时）
- 内存：单环境 Chromium ≤ 500MB（10 并行时整机 ≤ 6GB 为达标线）；主进程空闲 ≤ 200MB
- 磁盘：每环境数据目录 100–300MB；内核一份全局共享
- 数据安全：环境数据仅存本地，不采集、不上传用户浏览数据
- 合规：安装协议注明用途限制（合法多账号运营），分发保留各开源组件版权声明

## 8. 安全决策

| 项 | 决策 |
|---|---|
| 代理密码存储 | Electron `safeStorage` 加密后入库（DPAPI / Keychain）；不可用时降级为机器级混淆并明确提示用户 |
| 渲染进程隔离 | `contextIsolation: true`，preload 仅暴露白名单 API，禁用 `nodeIntegration` |
| 远程内容 | 环境浏览器内用户可访问任意网站（产品本职），但主界面不加载任何远程页面 |
| 卸载行为 | 卸载不删除 userData（保留用户登录态）；提供"彻底清除数据"入口 |

## 9. 错误处理矩阵

| 场景 | 用户可见行为 |
|---|---|
| 代理认证失败 | 阻止启动，提示"代理账号或密码错误" |
| 代理连接超时 / DNS 失败 | 阻止启动，区分提示，附"检查代理服务商"建议 |
| 内核缺失/损坏 | 跳转内核下载页，支持重试 |
| 磁盘空间不足 | 下载前拦截并提示清理 |
| SQLite 损坏 | 自动备份损坏文件为 `.bak`，重建空库并提示（环境列表为空属预期） |
| Chromium 启动后崩溃 | 状态回滚为 idle，通知栏提示 + 日志记录退出码 |
| 应用上次异常退出 | 启动时清理孤儿进程，无需用户干预 |

## 10. 已知边界（需写入用户文档）

- 指纹为 JS 层注入，防常规检测（FingerprintJS 级），不承诺对抗深度风控
- 同机多环境硬件指纹相似，平台严格禁止多号时有被关联风险
- Chromium 版本由 Playwright 锁定，需随版本发布定期升级
- 核心指纹固定意味着 Chromium 内核升级后 UA 版本号可能落后真实版本（injector 按内核版本生成，升级内核时旧环境 UA 与新内核版本存在不一致窗口，需在发布说明中说明）

## 11. 里程碑

| 阶段 | 内容 | 周期 | 验收锚点 |
|---|---|---|---|
| M1 | 脚手架 + 内核管理 + 环境隔离 + 启动/停止跑通 | 1 周 | 验收 1、2、6 |
| M2 | 代理配置 + 指纹两段式 + SQLite 持久化 | 1 周 | 验收 3、4、5 |
| M3 | 界面完善 + 并行稳定性 + Windows/macOS 打包 | 2 周 | 验收 7、8、9 |
| M4 | 内测 10 个种子客户，收集反馈 | 2 周 | 种子客户 NPS ≥ 30 |

## 12. 验收标准

1. 新建 3 个环境绑不同代理并行运行：同平台 3 个不同账号互不串号、互不掉线
2. 重启应用重开环境：登录态保留
3. 同环境两次启动，访问 browserleaks.com / creepjs：核心指纹字段（UA、屏幕、WebGL、硬件并发数）完全一致
4. 代理密码错误时：启动被阻止，提示明确指出"认证失败"
5. 更换代理且国家变化：弹窗确认后仅时区/语言变化，核心指纹不变
6. 关闭某环境浏览器窗口：该环境状态变 idle，其他环境不受影响
7. 10 环境并行 30 分钟：无崩溃、无串数据、界面状态实时准确
8. Windows / macOS 安装包可正常安装、运行、卸载；卸载后登录态保留
9. 断网状态下首次启动：内核下载给出明确失败提示，可重试

## 13. 开放问题（待决策）

1. 安装包是否预置内核（+约 250MB 体积，换首启即用）vs 首启下载（安装包小）？倾向后者
2. 是否支持代理 PAC / 订阅链接批量导入？
3. 对齐字段中的 geolocation 是否默认开启（部分站点会请求定位权限）？
4. 环境数量上限（免费/付费分层时如何限制）？
