# 任务文档修订执行计划（已获口头确认，含审查中新发现）

把对抗式审查确认的 16 处缺口写回 doc/tasks/，另加 1 处新发现共 17 项改动。**只改文档，不动 src/ 代码，不勾任何清单，不 git commit。**

## 逐文件改动

1. **01-scaffold.md**：T2 类型清单补 `EgressInfo`（proxy:test 返回类型）
2. **02-storage.md**：T4 补"密码明文仅限主进程内存、不落日志"；新增 T6 滚动日志器（logs/ 5MB×5，§9 崩溃退出码/孤儿清理/降级提示的依赖）
3. **03-kernel.md**：T1 补"revision 从 playwright-core browsers.json 读取，不硬编码（§3 版本锁定策略）"
4. **06-launcher.md**：T2 补"decryptSecret 解密代理密码（仅内存）+ CDP Browser.setDownloadBehavior 落地每环境下载目录"；新增 T7 应用退出钩子（before-quit 逐个优雅停止、5s 超时强杀，§6.6）
5. **07-env-manager.md**：T1 补出口国家来源与降级策略（有代理先 proxy:test / 无代理直连基准 / 测试失败允许保存+记日志）；T2 标注 align:confirm 为契约外新增通道；T4 补 last_launched_at 更新；新增 T7 `env:status`（§4 唯一漏认领通道）；新增 T8 `app:wipeData`（§8 彻底清除数据）
6. **08-ui.md**：T1 补"主界面不加载远程页面"注记；T2 补"直连"标注；T4 补 safeStorage 降级提示；T8 补 SQLite"已重置"提示；新增 T9 崩溃通知栏；新增 T10 彻底清除数据入口
7. **09-packaging.md**：T4 补用户文档（§10 已知边界 + 内核升级 UA 窗口说明）；T5 补性能量化锚点（启动≤5s、10并行≤6GB/30min）+ 内核升级发布流程；文末补 Linux 兼容注记（保留 target，不在 M3 验收）
8. **progress.md**：头部追加本次审查修订记录；新增"P1/P2 待办（未拆任务）"区（需求条目 7–11 + P2 四项）；**修正失实记录**——当前写"阶段 0 已完成（01 合同冻结）"，但代码中无 src/shared/types.ts、无 src/main/ipc.ts、脚手架 demo 未清、better-sqlite3 未装，改为"阶段 0 未开始，01 未完成"
9. **新建 doc/tasks/traceability.md**：需求↔任务追溯矩阵（§4 全通道、§5 存储、§6.1–6.7、§7、§8、§9 七行、§12 验收 1–9 → 任务 ID，审查补的条目单独标注），作为后续"不出问题"的持久审计物

## 完成后自检
- grep 确认全部勾选框仍为 `- [ ]`（未偷跑）；
- 对照 §4 契约表逐条确认 11+2+新增通道每条有唯一认领；
- 复核 §12 验收 1–9 每条在 traceability.md 中有锚点，输出最终差异清单。