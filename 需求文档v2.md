# 任务文件对抗式审查修补计划

## 现状
- `doc/tasks/01~09-*.md`、`progress.md`、`parallel-plan.md` 已由此前会话生成，结构与你的要求一致（每模块一个 md + 勾选清单 + progress.md 模块勾选表）。
- 代码仍是 electron-vite 原始脚手架（Versions.vue 未清），`playwright-core`/`fingerprint-generator`/`fingerprint-injector` 已装但 **缺 better-sqlite3**（由 01-T6 补）→ **所有勾选框保持未勾，正确，不动**。
- 本次只改文档：按对抗式审查结果修补缺口，不写代码、不提交 git。

## 对抗式审查结论（对照需求文档 §3~§12 全量核对）

### A 级：契约/流程硬伤（不补会做不出来）
1. **`env:status` IPC 通道无人认领**（§4 契约表 11 通道里唯一漏项）→ 补到 07
2. **应用退出时逐个优雅停止所有环境**（§6.6）完全没任务 → 06 新增 T7
3. **滚动日志系统无任务**（§5 logs/ 5MB×5；§9 崩溃退出码、孤儿清理都依赖"写日志"）→ 02 新增 T6
4. **下载目录隔离落地方式缺失**：Chromium 默认下载目录不在 profile 内，`userData/envs/{id}/downloads` 需要 CDP `Browser.setDownloadBehavior`（或等效）显式设置（§5/§6.3）→ 补进 06-T2
5. **`env:create` 时指纹地区输入来源未定义**（§6.5"以当时代理出口地区为输入"）：有代理→先 proxy:test 取 country；无代理→直连基准；测试失败的策略也需写明 → 补进 07-T1
6. **内核 revision 来源未定义**（§3"playwright-core 版本↔revision 一一对应"）：应从 `playwright-core/browsers.json` 读取而非硬编码 → 补进 03-T1

### B 级：错误矩阵/安全/验收覆盖缺口
7. 08 缺 SQLite 损坏"已重置"提示（§9 行 5；02 已返回标记但 UI 无人接）
8. 08 缺 Chromium 崩溃通知栏提示（§9 行 6；06-T6 只推送了事件）
9. 08 缺无代理环境"直连"标注（§6.4）
10. 08 缺 safeStorage 降级 `weak:true` 提示（§8"明确提示用户"）
11. 08 缺"彻底清除数据"入口；07 缺对应 IPC（§8）→ 07 新增 T8 `app:wipeData`
12. 07-T4 缺 `last_launched_at` 更新时机
13. 06-T2 缺启动前 `decryptSecret` 解密代理密码（仅主进程内存）说明
14. 09-T4 缺用户文档要求（§10 已知边界四条 + 内核升级 UA 不一致窗口说明）
15. 09-T5 缺性能锚点（启动 ≤5s、10 并行 30 分钟整机 ≤6GB，§7/验收 7）
16. 01-T2 类型清单缺 `EgressInfo`（proxy:test 返回类型）

### 审查确认无问题的部分
依赖顺序（01→02→03→04∥05→06→07→08→09）、§4 其余 10 通道归属、§6.2 内核流程、§6.5 两段式与只读约束、§9 矩阵主进程侧、§12 验收 1–9 均有对应任务。

## 修补动作（7 个文件小改 + 1 个新文件）

| 文件 | 改动 |
|---|---|
| `01-scaffold.md` | T2 类型清单补 `EgressInfo` |
| `02-storage.md` | 新增 T6：滚动日志器（logs/ 5MB×5，全模块唯一日志入口）；T4 补"解密仅主进程内存使用" |
| `03-kernel.md` | T1 补"revision 从 playwright-core browsers.json 读取，不硬编码" |
| `06-launcher.md` | T2 补"解密代理密码 + CDP 设置每环境下载目录"；新增 T7：应用退出钩子（before-quit 逐个优雅停止，5s 超时强杀） |
| `07-env-manager.md` | T1 补出口国家来源与降级策略；T4 补 last_launched_at；新增 T7 `env:status`；新增 T8 `app:wipeData`（标注 align:confirm/wipeData 为契约外新增通道） |
| `08-ui.md` | T2 补"直连"标注；T4 补 weak 降级提示；T8 补 SQLite 已重置提示；新增 T9 崩溃通知栏；新增 T10 彻底清除数据入口（二次确认）；T1 补"主界面不加载远程页面"注记 |
| `09-packaging.md` | T4 补用户文档（§10 四条边界 + UA 窗口说明）；T5 补性能锚点 + 内核升级发布流程说明；附注 Linux 打包预留（M3 只验收 win/mac） |
| `progress.md` | 新增"P1 待办（未拆任务）"区（需求条目 7–11）；追加一条修订记录（本次审查日期+缺口数） |
| **新建** `doc/tasks/traceability.md` | 需求追溯矩阵：§4 每条通道、§5 路径、§6.1–6.7、§7 锚点、§8 四项、§9 七行、§12 验收 1–9 → 任务 ID。这是"对抗式审查不出问题"的持久审计物，后续每完成一个任务可回查 |

## 完成后自检（对抗式复核）
1. 重读全部改动文件，逐条对照 §4 通道表（12+2 条）确认每条都有唯一认领且方向/参数一致；
2. grep 确认所有勾选框仍为 `- [ ]`（未偷跑）；
3. 确认 traceability.md 覆盖 §12 验收 1–9 每条至少一个任务锚点；
4. 汇报最终差异清单。

## 明确不做
不改任何 `src/` 代码；不勾任何清单；不 git commit（如需提交请说明）。
