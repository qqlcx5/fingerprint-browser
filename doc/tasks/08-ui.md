# 08 渲染层界面（Vue3）

> 依赖：01（可用 mock `window.api` 并行开发，07 完成后联调）
> 产出：`src/renderer/src/`
> 完成标准：§9 错误矩阵文案全覆盖；状态实时准确（验收 6/7 的界面部分）

## 任务清单

- [x] T1 布局骨架 + 视图划分：环境列表 / 内核下载 / 创建编辑弹窗；开发期 mock 层开关（注记：主界面不加载任何远程页面，§8）
- [x] T2 环境列表页：列 = 名称/代理摘要/状态徽标/最后启动时间；无代理环境代理摘要显示"直连"标注（§6.4）；订阅 `env:status-changed` 实时刷新
- [x] T3 创建/编辑弹窗：名称备注 + 代理表单（类型/主机/端口/账号密码）+「测试连接」按钮展示出口 IP/国家/延迟（§6.4）
- [x] T4 启动/停止按钮；运行中禁用删除；错误 toast（`AppError.code` → §9 文案映射表，缺 code 给兜底文案）；safeStorage 降级提示来自 `app:notices`（kind=weak_encryption，§8）
- [x] T5 删除二次确认弹窗：明确"配置与浏览数据不可恢复"
- [x] T6 内核下载遮罩页：进度条 + 体积提示（约 150–300MB）+ 失败重试（验收 9）
- [x] T7 国家变更确认弹窗：显示"地区从 A → B"，确认/取消分别走 `align:confirm` 两个分支（§6.5）
- [x] T8 空状态与首次引导：无环境 → 引导创建；内核缺失 → 自动跳下载页（§6.2）；挂载后调 `app:notices` 拉取，kind=db_reset → 提示"数据已重置，环境列表为空属预期"（§9）
- [x] T9 崩溃通知栏：订阅 `env:crashed`（`CrashedInfo{envId, exitCode}`）→ 通知栏提示并支持查看日志（§9 行 6）
- [x] T10 "彻底清除数据"入口：设置区按钮 → 二次确认 → 调 `app:wipeData`（§8；语义：清环境数据，内核与日志保留）

## 验证记录（2026-09-07）

- typecheck / lint / build / E2E 冒烟全绿；真实界面观感需 `pnpm dev` 人工确认
- 契约补充（协调者操作，需回写需求文档 §4 IPC 表）：新增 `env:get`（编辑弹窗需要完整 Env 含 hasPassword；列表只有摘要）

## 实现偏离记录

1. **T1 mock 层开关**：未做。07 已完成可直接联调，mock 属过度设计
2. **T9 崩溃通知**：以 toast 呈现（含"详情见日志"提示，日志位于 userData/logs）；"查看日志"入口属 P1 日志查看页，未在本次实现
3. **T10 彻底清除数据**：入口放在列表页头部（无独立设置页），二次确认弹窗内注明"内核与日志保留"
4. **编辑代理语义**：代理字段未改动 → 提交省略 proxyConfig（原密码保持）；改动过 → 密码必填（安全不回显，防意外清空）
