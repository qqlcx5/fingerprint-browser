# 账号环境管理器 PRD V2.0

## 1. 产品定位

本产品是面向已获 TikTok Shop 平台授权的电商运营人员的本地桌面账号环境管理器。它以独立 Chromium Profile 为单位管理店铺登录环境，并为每个环境固定绑定一个经验证的代理出口 IP。

一期目标是让操作者能够稳定地创建、启动、备份和审计多个彼此独立的店铺环境，支持平台的两步验证和子账号角色运营方式；产品不保存平台账号密码或验证码，不提供自动化操作能力。TOTP 密钥默认不存储；仅在操作者显式启用、操作系统安全存储可用且完成二次确认时，才允许为单个环境加密保存。

### 1.1 一期目标

1. 账号环境隔离：每个环境拥有独立的浏览器用户目录、Cookie、缓存、IndexedDB 和下载目录。
2. 独立代理 IP 绑定：每个业务环境必须绑定一个代理配置，并记录首次验证的出口 IP；同一出口 IP 默认不得绑定多个业务环境。
3. 轻量配置差异化：每个环境首次创建时生成并固化浏览器配置快照，包含 UA、语言、时区、窗口尺寸和分辨率；重启时保持一致。
4. 账号安全运营支撑：展示 2SV 启用状态、已配置验证方式数量、登录提醒与未知设备清理待办；引导使用平台子账号和角色授权，禁止共享密码。
5. 基础运营管理：支持环境 CRUD、代理检测、启动与停止、按“站点 -> 店铺”分组、搜索、配置导入导出和审计日志。

### 1.2 非目标

以下能力不属于本产品范围，也不能作为宣传或验收指标：

1. 绕过平台风控、规避账号关联或保证平台通过率。
2. TLS/JA3 修改、Chromium 内核修改、DNS 系统级拦截。
3. 自动化特征隐藏、行为模拟、随机网络抖动、脚本化平台操作。
4. Canvas、Audio、WebGL 等渲染指纹噪声注入或随机微偏移。
5. 代替平台的两步验证、身份重新验证、子账号角色权限或账号安全机制。

固定出口 IP、每环境唯一出口 IP 和代理类型要求是本产品为降低日常运维风险设定的操作约束，不是 TikTok Shop 官方发布的“一 IP 一店”或设备指纹要求。

注：TikTok Shop 等平台的官方政策并未强制规定“一店一 IP”或设备指纹要求。本产品中的出口 IP 唯一性与配置固化属于防关联最佳实践约定，而非平台强制要求；产品定位为官方合规的本地登录环境管理器。

## 2. 核心概念与约束

### 2.1 环境

环境是一个店铺账号的本地运行容器。一个环境对应一个业务店铺的登录态，具有唯一 ID，且只允许启动一个浏览器实例。环境必须记录 TikTok Shop 站点、店铺标识和可选的平台子账号角色备注；界面按“站点 -> 店铺”两级组织。

环境目录结构如下：

```text
envs/{environmentId}/
  profile/       Chromium 用户数据目录
  downloads/     该环境的下载目录
```

业务环境不得以直连方式启动。无代理启动只允许开发诊断用途，且不能保存平台登录态。

环境应标注所属平台站点（如 TikTok Shop 的 US、UK、SEA 等）。环境的代理出口国家应与店铺站点一致；不一致时启动前给出警告，由操作者确认后继续，不自动阻断。

### 2.2 代理绑定

代理绑定是环境和代理出口之间的持久化关系，包含协议、主机、端口、认证凭据、已验证出口 IP、出口国家和最后验证时间。业务环境仅接受静态或粘性住宅/ISP 代理；数据中心代理不满足一期运营标准。

1. 每个业务环境只能有一个启用中的代理绑定。
2. 创建环境和更换代理时必须先完成连通性及出口 IP 检测。
3. 检测到的出口 IP 已被另一业务环境绑定时，拒绝保存并提示操作者选择其他代理。
4. 启动时出口 IP 与已绑定 IP 不一致时，阻断启动。操作者必须显式执行“更换代理绑定”操作，重新检测并留下审计记录。
5. 支持 HTTP、HTTPS 和 SOCKS5 代理；代理账号密码仅在本机加密保存，界面和导出文件不得回显明文密码。

### 2.3 浏览器配置快照

浏览器配置快照用于保持一个环境在多次启动时的客户端配置稳定，而不是伪造或规避检测。快照包含：

1. 桌面 Chrome UA 与平台类型。
2. 语言、时区和可选的城市级地理位置。
3. 窗口尺寸、屏幕分辨率和设备像素比。

创建后，快照默认只读。编辑快照须在环境关闭时进行，生成新的配置版本并记录原因；不得在每次启动时随机改变配置。代理出口国家与店铺站点不一致时，启动前应显示明显警告但不阻断；操作者需自行确认该店铺的实际运营区域和平台规则。

## 3. 功能需求

### 3.1 环境管理

1. 创建环境：填写站点、店铺标识、名称、备注、分组和代理；完成代理检测后生成配置快照与独立目录。
2. 查看环境：展示站点、店铺标识、平台子账号角色备注、运行状态、代理摘要、已绑定出口 IP、出口国家、最后验证时间和最后启动时间。
3. 编辑环境：可编辑站点、店铺标识、名称、备注、分组和角色备注；修改代理或浏览器配置必须在环境停止后执行。
4. 启动与停止：环境启动后打开独立 Chromium 窗口；关闭最后一个窗口后环境状态回到停止。
5. 删除环境：必须二次确认；删除 Profile、下载目录、环境配置与对应审计数据。
6. 分组、搜索和批量操作：支持按站点、店铺标识、名称、分组、运行状态和出口国家筛选；支持批量导出及批量删除确认。
7. 两步验证支撑：环境详情支持记录 2SV 启用状态、验证方式数量、手机号绑定状态和登录提醒状态。可选保存 TOTP 密钥时，必须使用系统安全存储、每次读取要求操作系统重新认证、不在进程日志/崩溃报告/导出文件中出现，并提供单环境立即清除入口。

### 3.2 代理检测与绑定

1. 提供“测试代理”操作，返回出口 IP、国家、延迟和明确错误类型：认证失败、超时、DNS/解析失败、协议错误。
2. 绑定前应使用至少两个可配置的出口检测源；任一成功可判定代理可用，检测结果及来源写入日志。
3. 创建、修改和启动时均校验代理绑定；启动前检测失败或出口 IP 变化时，不创建浏览器上下文。
4. 支持 SOCKS5 认证代理。若 Chromium 不支持所需认证方式，可使用仅监听本机的中继服务；中继生命周期必须与环境一致，且不对局域网开放端口。
5. 代理变更记录旧出口 IP、新出口 IP、操作者确认时间和变更原因。
6. 支持通过 CSV 批量导入代理清单；导入后必须执行代理池预验证，展示出口 IP、国家、延迟、重复出口 IP 和不可分配原因。仅通过预验证且符合一期代理类型约束的条目可以批量分配给环境。

### 3.3 浏览器环境隔离

1. 每个环境必须以独立 Profile 路径启动持久化 Chromium 上下文。
2. Cookie、缓存、LocalStorage、IndexedDB、Service Worker 和下载文件不得在环境间共享。
3. 环境关闭并重新启动后，应用必须保留同一环境的本地 Profile 和登录态数据，除非操作者主动清除或平台使其失效。平台可自主要求再次登录、2SV 或账号重新验证；应用不得规避该流程，应显示重新验证待办并保留环境与安全状态。
4. 应在应用启动时清理异常退出遗留的浏览器进程，避免占用其他环境的 Profile。

### 3.4 轻量浏览器配置

1. 创建环境时，根据所选代理出口国家生成语言、时区与 UA 的合理组合；出口国家未知时使用默认英文桌面配置，并在界面提示。
2. 启动时将语言、时区、窗口尺寸和 UA 应用于该环境的 Chromium 上下文。
3. 当代理出口国家变化且操作者完成重新绑定后，应用提示更新语言、时区和地理位置；核心浏览器配置不自动随机重置。
4. 不注入渲染噪声，不修改 TLS 握手，不模拟用户行为，不承诺对第三方检测系统的结果。

### 3.5 备份、导入与审计

1. 导出环境配置时不包含 Cookie、缓存、登录态、代理密码、TOTP 密钥、恢复码、验证码和审计日志；导入后需要重新填写代理认证信息、重新验证出口 IP，并重新确认 2SV 安全状态。
2. 记录环境创建、启动、停止、代理检测、代理变更、配置版本变更、2SV 状态变更、TOTP 密钥启用/清除、导入和删除事件；日志不得记录任何凭据或动态验证码。
3. 审计日志仅保存在本机，支持按环境和时间筛选；默认不记录访问页面 URL、页面内容、键盘输入、账号密码或验证码。

## 4. 数据模型

### 4.1 Environment

| 字段 | 说明 |
| --- | --- |
| id | 环境唯一 ID |
| site | TikTok Shop 站点代码，如 `US` |
| shopIdentifier | 店铺标识或内部店铺编号，不保存平台密码 |
| roleNote | 平台子账号角色备注，如主管理员、财务或客服 |
| name / remark / group | 用户管理信息；分组按站点 -> 店铺组织 |
| profilePath / downloadsPath | 独立本地目录 |
| browserConfig | 版本化浏览器配置快照 |
| securityStatus | 2SV、手机号、登录提醒和最近安全检查状态，不含验证凭据 |
| totpSecretRef | 可选：系统安全存储中的单环境 TOTP 密钥引用；SQLite 仅保存引用，默认无值 |
| status | 仅内存保存：idle、starting、running、stopping |
| createdAt / updatedAt / lastLaunchedAt | 生命周期时间 |

### 4.2 ProxyBinding

| 字段 | 说明 |
| --- | --- |
| environmentId | 所属环境 ID，唯一 |
| protocol / host / port | 代理端点 |
| networkClass | 运营者声明并经采购记录核验的 `static_residential`、`sticky_residential` 或 `isp`；不是平台规则 |
| credentialSecret | 系统安全存储中的认证凭据引用 |
| expectedEgressIp | 已确认且唯一的出口 IP |
| country | 最近确认的 ISO 国家代码 |
| verifiedAt | 最近成功检测时间 |
| changedAt / changeReason | 最近一次重绑定信息 |

数据库应对 `expectedEgressIp` 建立唯一索引，仅允许空值用于开发诊断环境。

## 5. 启动流程

1. 确认环境当前为停止状态，并确保 Chromium 内核可用。
2. 读取环境代理绑定并测试代理出口。
3. 验证出口 IP 与 `expectedEgressIp` 相同；不相同或检测失败则停止流程并展示原因。
4. 以该环境的独立 Profile 和下载目录创建持久化 Chromium 上下文。
5. 应用已保存的语言、时区、UA 和窗口配置；若 2SV、手机号绑定、登录提醒或安全检查状态未完成，展示待办但不阻断浏览器启动。
6. 记录启动审计日志并更新状态。浏览器窗口关闭、崩溃或用户主动停止时，释放浏览器上下文和本地代理中继，状态回到停止。

## 6. 安全与隐私

1. Electron 主窗口必须启用 `contextIsolation`、禁用 `nodeIntegration`，预加载层仅暴露白名单 IPC API。
2. 代理密码使用操作系统安全存储加密；安全存储不可用时需明确提示，且默认不允许保存密码。
3. TOTP 密钥为可选高敏感数据：默认关闭，启用和读取时要求操作系统认证；不得写入 SQLite、环境导出、日志、剪贴板、崩溃报告或自动备份。删除环境、解除 2SV 管理或执行清除本地数据时必须同步销毁密钥。
4. 主界面不得加载远程网页；浏览器环境与管理主窗口的权限边界必须分离。
5. 所有业务数据默认仅保存在本机。删除环境或执行“清除本地数据”时，必须明确显示会删除的范围。
6. 产品使用说明必须提示操作者遵守 TikTok Shop 平台规则、账号授权范围和适用的数据保护法律；团队协作应使用平台子账号和角色授权，不得共享主账号密码。

## 7. 非功能要求与验收

### 7.1 支持范围

1. 一期支持 Windows x64 和 macOS arm64。
2. 发布验收基准为 32GB 内存、SSD、稳定网络；至少验证 12 个环境并行运行 30 分钟的稳定性，并记录内存与 CPU 占用数据。16GB 设备可运行产品，但不承诺支持 12 个并发环境。
3. 浏览器内核已就绪时，单环境冷启动目标不超过 10 秒；性能数据须标明硬件、内核版本和测试页面，不能以平台风控结果作为性能指标。

### 7.2 验收用例

1. 创建两个环境并分别登录测试站点后，环境 A 的 Cookie、LocalStorage 和下载文件在环境 B 中不可见。
2. 同一环境重启三次后，UA、语言、时区、窗口尺寸和已保存配置版本保持一致；本地 Profile 中的登录态未被应用主动清除。若测试站点要求重新验证，应用显示待办且不干预验证流程。
3. HTTP、HTTPS 和 SOCKS5 认证代理均能完成出口检测；认证失败、超时和协议错误能够给出不同错误提示。
4. 当环境 A 已绑定出口 IP 后，环境 B 尝试绑定同一出口 IP 会被拒绝。
5. 已绑定环境启动时检测到出口 IP 变化，浏览器不会启动；完成显式重新绑定后才可启动。
6. 批量导入 12 条代理时，系统展示预验证结果、重复出口 IP 和不可分配原因；导出的配置文件不包含登录态、Cookie、代理密码、TOTP 密钥、恢复码、验证码或审计日志。
7. 在基准设备上运行 12 个环境 30 分钟后，无环境串用 Profile、无状态卡死，且应用保留可诊断日志。
8. 环境详情可记录 2SV、手机号绑定、登录提醒与安全检查待办；未启用 TOTP 管理时系统不保存该密钥。启用后，密钥无法从 SQLite、日志、导出文件或剪贴板读取，且在删除环境时被销毁。

## 8. 开发策略与参考边界

1. 以当前项目为主线继续开发：保留 Playwright 持久化 Chromium 上下文、环境独立目录、SQLite、本地日志、代理出口检测和 SOCKS5 认证中继。
2. Mirage Browser 仅用于参考环境管理交互、代理列表和批量操作，不迁移其代码库或作为新项目模板。
3. 不接入 Python BrowserForge。当前项目已使用 Node.js 指纹配置依赖；一期只保留稳定、可持久化的基础浏览器配置。
4. 第一开发优先级为 ProxyBinding 数据模型、出口 IP 唯一校验、启动阻断、代理批量预验证和代理变更审计；第二优先级为店铺/站点/角色字段、2SV 安全状态、界面展示与导入导出限制；第三优先级为可选 TOTP 密钥的系统安全存储、打包与跨平台实机验收。

## 9. 已知风险

1. 部分代理供应商使用轮换出口 IP，不适合“一环境一固定出口 IP”的一期约束。静态或粘性住宅/ISP 代理、出口 IP 唯一性和数据中心代理限制均为本产品的运营规则，不是 TikTok Shop 的公开硬性规定；采购和使用前仍需核验各站点的最新平台规则。
2. 平台可基于自身安全策略要求重新登录、两步验证或账号重新验证；本地 Profile 持久化不能保证绕过或消除这些流程。
3. 可选 TOTP 管理会集中高敏感密钥。未满足系统安全存储、操作系统认证与销毁验证要求时，该功能不得上线。
4. 代理检测服务可能短时不可用，因此检测源需要可配置、可替换，并记录检测失败原因。
5. 浏览器 Profile 隔离只能隔离本产品管理的数据，不能替代操作系统账户隔离、企业网络策略或平台自身的账号治理。

## 10. 官方政策参考

以下页面为需求中的账号安全、两步验证和团队协作要求提供依据；均为 TikTok Shop Seller University 美国站页面，访问日期为 2026-09-09。不同站点规则可能不同，上线前必须按目标站点再次核验。

1. [Securing your Account with Two-Step Verification](https://seller-us.tiktok.com/university/essay?knowledge_id=4319053571245870)：要求卖家绑定手机号并启用 2SV；建议配置至少两种验证方式。
2. [How to Protect Against Unauthorized Account Access](https://seller-us.tiktok.com/university/essay?knowledge_id=5226056652146475)：建议使用唯一强密码、启用 2SV、清理未知设备并开启登录提醒。
3. [How to Manage Team Access to Organization Account on TikTok Shop](https://seller-us.tiktok.com/university/essay?knowledge_id=2500395617929015)：支持以子账号及角色授予、修改和撤销组织账号访问权限。
