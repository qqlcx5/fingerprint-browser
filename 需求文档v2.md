电商对抗式审查专用 Electron 指纹浏览器 PRD V2.0

一、文档基础

1. 产品定位：基于Electron封装标准Chromium内核，不修改内核源码，复用成熟开源模块，面向电商多账号场景，通过对抗式风控适配，实现单设备多环境无关联、高通过率运营
2. 核心约束：原生支持Socks5/HTTPS代理，指纹与代理IP属地强绑定，兼容Windows10+、macOS12+，商用开源组件均采用MIT/Apache2.0协议
3. 参考开源：Mirage Browser、BrowserForge、GhostShell Fingerprint 独立preload模块

二、核心目标

1. 开发效率：复用开源成熟模块，定制开发量压缩至原自主方案40%
2. 风控指标：主流电商平台账号关联率≤0.8%，指纹重启漂移率≤0.3%，对抗式审查通过率≥92%
3. 性能指标：单环境冷启动≤3秒，10环境并发运行CPU占用≤20%

三、全量功能模块

1. 环境管理模块

- 基础CRUD：环境创建、启动、编辑、备份、删除，每个环境绑定唯一ID与独立userData目录
- 启动全链路预检：依次校验代理连通性、IP属地、DNS无泄漏、指纹哈希一致性、JA3指纹合规性，任意一项异常阻断启动并给出修正指引
- 批量运营工具：支持批量导入代理、批量生成同属地环境、批量备份配置，适配电商多店铺上号场景

2. 对抗级指纹混淆模块

- 基础一致性指纹：由BrowserForge按代理IP属地生成固定种子，覆盖UA、时区、语言、分辨率、字体列表，每个环境在开源模板基础上增加0.5%-1%的随机微偏移，规避开源工具批量特征
- 高级隐蔽指纹：复用GhostShell preload覆写逻辑，固定种子生成Canvas纹理噪声、WebGL参数、Audio频谱指纹，强制拦截WebRTC接口屏蔽局域网IP
- 自动化特征清零：移除navigator.webdriver、Electron专属标识、Puppeteer残留字段，通过CDP注入抹除Chromium自动化特征标记

3. 多协议防泄漏代理模块

- 全协议支持：兼容Socks5、HTTPS/HTTP带认证代理，复用Mirage的Mihomo适配层
- 三重防泄漏：强制禁用WebRTC、拦截系统本地DNS请求、代理链路全程走隧道DNS，杜绝IP、DNS泄漏
- 属地自动对齐：启动时通过GeoIP解析代理IP属地，自动同步修改时区、语言、UA、JA3指纹，不匹配则强制修正后才可进入页面

4. 强隔离与进程模块

- 存储隔离：单环境绑定独立persist partition，Cookie、IndexedDB、缓存文件完全物理隔离
- 进程隔离：单环境独占渲染进程，禁止跨环境上下文共享、剪贴板跨环境传递，彻底切断残留关联路径

5. 电商对抗式审查适配模块

- TLS/JA3指纹对齐：通过Chromium启动参数微调TLS握手特征，匹配对应属地普通Chrome的JA3指纹，避免Electron原生TLS特征被标记
- 加载时序模拟：给页面资源加载增加100-500ms随机网络抖动，模拟真实用户的非匀速加载节奏
- 行为白名单校验：新环境首次登录、日常操作的停留时长、点击路径、输入速度，对齐对应电商平台真实买家行为区间，超出区间则弹窗预警
- 风控日志审计：全量记录环境启动、代理变更、访问域名、平台风控弹窗、行为异常事件，支持按账号导出排查

四、非功能与验收标准

1. 安全合规：禁用nodeIntegration、开启contextIsolation，IPC接口加Token认证，开源组件协议合规审计全覆盖
2. 验收用例：同环境重启3次指纹哈希一致；Socks5/HTTPS代理下WebRTC、DNS均无泄漏；美区代理环境时区、JA3、语言全匹配纽约属地；跨环境登录同电商平台无互踢、无关联提示；对抗式风控扫描无工具特征标记

五、开发与开源复用策略

1. 主框架：基于Mirage Browser二次开发，替换通用UI为电商运营专用界面
2. 指纹层：接入BrowserForge生成属地基础指纹，叠加微偏移规避批量特征，复用GhostShell preload做高级指纹覆写
3. 代理层：保留Mirage的Socks5/HTTPS适配，新增DNS防泄漏、JA3对齐、属地自动校验逻辑
4. 定制开发：仅新增对抗式审查专属的TLS微调、行为白名单、全链路预检功能，其余复用开源成熟代码

六、风险与规避

1. 开源批量特征风险：每个环境指纹加微偏移，定期更新开源模板特征库
2. TLS指纹暴露风险：启动前校验JA3哈希，不匹配则自动微调握手参数
3. DNS泄漏风险：强制拦截系统DNS请求，仅允许代理隧道DNS解析
4. 行为异常风险：内置电商平台行为区间库，操作超出阈值实时预警
