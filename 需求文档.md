# 多账号环境隔离浏览器 — 需求文档

> 版本 v1.0 / 2026-09-07 / 内部资料

## 1. 项目定位

桌面端多账号浏览器管理工具，面向电商客服、社媒运营等合法多账号场景。

核心能力：**账号环境隔离 + 独立代理 IP 绑定 + 轻量指纹差异化**。

不做：深度防关联（对抗 Cloudflare/银行级风控）。产品宣传禁止使用"防封号""绕过风控"等表述。

## 2. 技术选型（全部宽松协议，可商用分发）

| 组件 | 选型 | 协议 |
|---|---|---|
| 界面壳 | Electron | MIT |
| 脚手架 | electron-vite（create-electron，Vue3 + TS） | MIT |
| 启动层 | playwright-core | Apache-2.0 |
| 浏览器内核 | Playwright 管理的 Chromium 二进制 | BSD-3 |
| 指纹差异化 | apify/fingerprint-generator + fingerprint-injector | MIT/Apache-2.0 |
| 本地存储 | SQLite（better-sqlite3） | MIT |
| 打包 | electron-builder | MIT |

明确不使用：CloakBrowser（二进制禁止再分发）、VirtualBrowser（内核授权不明）。

## 3. 架构

```
Electron 渲染进程（Vue3 界面）
    ↕ IPC（preload 桥接，contextIsolation 开启）
Electron 主进程
    ├─ 环境管理（CRUD，SQLite）
    ├─ launchEnv(envId) → playwright-core + fingerprint-suite
    └─ Chromium 下载管理（npx playwright install chromium，锁定版本）
```

## 4. 功能需求

### P0（MVP，必须有）

1. **环境管理**：创建/删除/重命名环境；每环境一条记录（id、名称、备注、指纹 JSON、代理配置、创建时间）
2. **环境隔离**：每环境独立 `userDataDir`，cookie/localStorage/登录态/缓存完全隔离且持久化
3. **代理配置**：每环境支持 HTTP/HTTPS/SOCKS5，含账号密码认证；启动前连通性测试（测出口 IP 与地区）
4. **一键启动/停止**：启动后打开浏览器窗口；显示运行状态；支持主动关闭
5. **指纹生成与固定**：每环境首次创建时生成一份指纹（UA/GPU/屏幕/语言/时区），存 SQLite，**此后永久复用，不得漂移**；时区/语言/地理位置自动对齐代理出口 IP
6. **多环境并行**：支持同时运行 ≥10 个环境，互不影响

### P1（第二迭代）

7. 环境分组/搜索/批量删除
8. 环境配置导出/导入（JSON，不含 cookie）
9. 指纹手动编辑（UA、分辨率等）
10. 开机启动、系统托盘

### P2（商业化阶段）

11. 云端同步环境配置
12. 团队协作与权限
13. 授权/计费（按环境数订阅）
14. 自动更新（electron-updater）

## 5. 非功能需求

- 平台：Windows x64 优先，macOS arm64 次之，Linux 兼容
- 启动单个环境 ≤ 5s（本地 Chromium 已就绪）
- 数据安全：环境数据仅存本地，不采集用户浏览数据
- 合规：安装协议中注明用途限制（合法多账号运营），分发时保留各开源组件版权声明

## 6. 已知边界（需写入用户文档）

- 指纹为 JS 层注入，防常规检测（FingerprintJS 级），不承诺对抗深度风控
- 同机多环境硬件指纹相似，平台严格禁止多号时有被关联风险
- Chromium 版本由 Playwright 锁定，需随版本发布定期升级

## 7. 里程碑

| 阶段 | 内容 | 周期 |
|---|---|---|
| M1 | 脚手架 + 环境隔离 + 启动/停止跑通 | 1 周 |
| M2 | 代理配置 + 指纹固定 + SQLite 持久化 | 1 周 |
| M3 | 界面完善 + 并行稳定性 + Windows/macOS 打包 | 2 周 |
| M4 | 内测 10 个种子客户，收集反馈 | 2 周 |

## 8. 验收标准

- 新建 3 个环境，分别绑定不同代理，同时运行：三环境登录同一平台的不同账号，互不串号、互不掉线
- 重启应用后重开环境：登录态保留
- 关闭代理后环境无法启动并给出明确报错
- Windows / macOS 安装包均可正常安装运行
